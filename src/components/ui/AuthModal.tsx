import React from 'react';

/**
 * AuthModal has been disabled per user instruction.
 * All watermark removal, video inpainting, and compression features
 * are 100% free and do not prompt for Google sign-in.
 */
export const AuthModal: React.FC = () => {
  return null;
};
